PARENT_PATH=https://github.com/yixiang/share/blob/master/setup
if [ "`uname`" = "Darwin" ]; then
	echo "Setting up as Mac environment"
	curl -L "$PARENT_PATH/mac/run" | bash
else
	echo "Setting up as Linux environment"
	curl -L "$PARENT_PATH/linux/run" | bash
fi
