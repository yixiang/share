export HOST=http://dl.dropbox.com/u/2833661/setup
export OS=linux

curl -L "$HOST/common/vimrc" > ~/.vimrc
curl -L "$HOST/$OS/alias" >> ~/.bashrc
curl -L "$HOST/common/alias" >> ~/.bashrc
source ~/.bashrc
