export MASTER_PATH=https://raw.githubusercontent.com/yixiang/share/master/
export OS_NAME=mac

curl -L "$MASTER_PATH/vim/vimrc" > ~/.vimrc
curl -L "$MASTER_PATH/setup/$OS_NAME/alias" >> ~/.bash_profile
curl -L "$MASTER_PATH/setup/common/alias" >> ~/.bash_profile
